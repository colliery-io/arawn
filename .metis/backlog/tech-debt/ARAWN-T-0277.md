---
id: review-and-document-default-agent
level: task
title: "Review and document default agent configuration values"
short_code: "ARAWN-T-0277"
created_at: 2026-03-06T13:54:11.361957+00:00
updated_at: 2026-03-08T14:31:32.529401+00:00
parent: 
blocked_by: []
archived: false

tags:
  - "#task"
  - "#tech-debt"
  - "#phase/completed"


exit_criteria_met: false
initiative_id: NULL
---

# Review and document default agent configuration values

## Objective

Audit all default configuration values for the agent and ensure they're sensible. Current defaults may be too restrictive or undocumented. The orchestrator uses `max_iterations=1` by design (single-step turns), but this produces noisy "Max iterations exceeded" warnings in logs that look like errors.

### Type
- [x] Tech Debt - Code improvement or refactoring

### Priority
- [x] P1 - High (important for user experience)

### Current Defaults (arawn-types defaults module)
- `MAX_ITERATIONS`: 25 (main agent), 1 (orchestrator sub-agents)
- `max_tokens`: 65536 (from config.toml)
- `shell_timeout`, `web_timeout`, `max_output_bytes`: need audit

### Known Issues
- Orchestrator agents use `max_iterations=1` by design (check context size after every tool call for compaction). This is correct, but produces WARN-level "Max iterations exceeded" logs that look like errors — should be DEBUG since it's expected behavior in the orchestrator path
- No documentation of what each config value does and what reasonable ranges are
- Plugin-spawned agents inherit `default_max_iterations` from `[agent.default]` config — unclear if this is always desirable

## Acceptance Criteria

## Acceptance Criteria

## Acceptance Criteria

## Acceptance Criteria

- [x] All default values reviewed and confirmed sensible for typical usage
- [x] Orchestrator's `max_iterations=1` pattern doesn't produce misleading WARN logs
- [x] Config values documented (inline comments in defaults module, or config reference doc)
- [x] Default config.toml generated by `arawn init` includes commented-out options with explanations

## Status Updates

### Completed

**Defaults audit**: All defaults reviewed — values are sensible for typical usage. No changes needed to actual default values.

**Log level fix**: Downgraded "Max iterations exceeded" from `warn!` → `debug!` in both `agent.rs` (line 213) and `stream.rs` (line 198). This was the main user-facing issue — orchestrator sub-agents with `max_iterations=1` produced WARN-level noise that looked like errors but was expected behavior.

**Defaults module documentation**: Added doc comments to every constant in `arawn-types/src/config.rs::defaults` module. Each constant now documents its purpose, unit, and which config.toml key overrides it.

**Config init template expansion**: Expanded `arawn config init` template in `commands/config.rs` with documented sections for `[agent.default]`, `[tools.shell]`, `[tools.web]`, `[tools.output]`, and `[server]` — all commented out with inline explanations of each value.

**Test fix**: Fixed 7 pre-existing test failures in `state.rs` ownership tests — they didn't register connections as active before claiming ownership, so the dead-owner eviction logic (added in prior session) evicted them. Added a new `test_dead_owner_eviction` test covering the eviction path.

**Files modified**:
- `crates/arawn-types/src/config.rs` — doc comments on defaults module
- `crates/arawn-agent/src/agent.rs` — warn→debug for max iterations
- `crates/arawn-agent/src/stream.rs` — added debug log for max iterations in streaming path
- `crates/arawn/src/commands/config.rs` — expanded config init template
- `crates/arawn-server/src/state.rs` — fixed ownership tests + new dead-owner eviction test