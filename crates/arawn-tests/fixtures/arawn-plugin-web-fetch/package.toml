[package]
name = "arawn-plugin-web-fetch"
version = "0.1.0"
interface = "arawn-tool-plugin"
interface_version = 1
extension = "arawn_tool"

[metadata]
category = "web"
description = "Fetch URL content and return as text"
