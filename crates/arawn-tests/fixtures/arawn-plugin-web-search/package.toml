[package]
name = "arawn-plugin-web-search"
version = "0.1.0"
interface = "arawn-tool-plugin"
interface_version = 1
extension = "arawn_tool"

[metadata]
category = "web"
description = "Search the web and return results with titles, URLs, and snippets"
