[package]
name = "arawn-plugin-web-search"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
arawn-tool-plugin = { path = "../../crates/arawn-tool-plugin" }
reqwest = { version = "0.12", features = ["blocking"] }
serde_json = "1"
